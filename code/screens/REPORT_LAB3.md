# Отчет по Лабораторной работе №3: Исследование архитектуры проекта KitchenMate

## Часть 1. Проектирование архитектуры (To Be)
На высоком уровне абстракции система спроектирована как классическое многослойное приложение.

1. **Тип приложения:** Desktop GUI (на базе Tkinter).
2. **Стратегия развертывания:** Standalone (локальная установка),не распределенная.
   .Почему так:
   Локальная обработка: Все компоненты приложения (интерфейс, логика и база данных SQLite) устанавливаются и запускаются на одном узле — компьютере пользователя.
   Отсутствие серверной части: У проекта нет собственного выделенного сервера приложений или сервера базы данных, который требовал бы отдельного развертывания и поддержки.
Автономность данных: База данных SQLite является встраиваемой (файл kitchen_mate.db лежит в папке с программой), что исключает необходимость сетевого взаимодействия для хранения основных данных.
Внешний API как зависимость: Использование внешнего API (TheMealDB) не делает систему распределенной в плане развертывания, так как мы не разворачиваем этот сервер сами. Наше приложение выступает в роли «толстого клиента», который просто потребляет готовый сервис.
4. **Технологии:** Python 3.13, SQLite (БД), TheMealDB (внешний API).
5. **Качество:** Высокая отзывчивость интерфейса и надежное хранение данных.
6. **Сквозная функциональность:** Единая система авторизации и централизованная обработка ошибок API.

**Схема слоев (To Be):**
![Архитектура To Be](https://github.com/eofid/KitchenMate/blob/main/docs/architecture_to_be.png)

---

## Часть 2. Анализ текущей архитектуры (As Is)
Текущая реализация соответствует модульному подходу, где каждый экран вынесен в отдельный класс в папке `screens/`.

**Диаграмма (As Is):**
![Архитектура As Is](https://github.com/eofid/KitchenMate/blob/main/docs/architecture_as_is.png)

---

## Часть 3. Сравнение и рефакторинг

### 1. Сравнение архитектур
Схема **"As Is"** наглядно демонстрирует выполнение принципов из схемы **"To Be"**:
* Интерфейс (UI Layer) полностью отделен от логики работы с БД.
* Все запросы проходят через центральный контроллер `KitchenMateApp`.

### 2. Отличия и причины
* **Отличие:** В текущем коде авторизация вынесена в JSON-файл, в то время как остальные данные хранятся в SQLite.
* **Причина:** Использование JSON позволило быстро реализовать MVP (минимально жизнеспособный продукт) во время первого спринта.

### 3. Пути улучшения (Рефакторинг)
1. **Унификация БД:** Перенос данных пользователей из JSON в основную базу SQLite.
2. **Абстракция API:** Создание отдельного класса-провайдера для работы с интернет-рецептами (уменьшение связанности кода).
3. **Интерфейсы:** Использование базового класса для всех экранов для исключения дублирования кода импортов стилей.

## Вывод
Архитектурное решение проекта KitchenMate является масштабируемым. Выбранная модель "Звезда" позволила безболезненно добавить экран логина и расширить систему поиска в ходе разработки.
